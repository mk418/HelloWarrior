# macOS Finder metadata — gets sprinkled everywhere by the OS.
.DS_Store

# Claude Code session scratch (workflow scripts, transcripts) — not project files.
.claude/

# Generated at release time by .github/changelog.sh and consumed by the
# packager (see .pkgmeta). Ships in the zip, but is never committed.
CHANGELOG.md
